# power-apps

- **Power Pages portal** — external/customer entry (Entra External ID + Conditional Access).
- **Canvas app** — staff/field experience (Intune-managed), talks to Dataverse + Copilot Studio.
- **Model-driven app** — back-office over the `sentinel_*` tables (missions, telemetry, agents).

Package as part of the JABB Sentinel solution; promote Dev→Test→Prod via pipelines.
